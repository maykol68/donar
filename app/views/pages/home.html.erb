<div>
  <h1 class="font-bold text-4xl">Pages#home</h1>
  <p>Find me in app/views/pages/home.html.erb</p>


<% if current_user %>
    <p>Hello <%= current_user.email %>!</p>
    <p> You are a [<%= current_user.role %>]
    <button><%= link_to "Donate", checkout_path%></button>
    <% if current_user.admin?%>
    <br />
        there are <%user.count%> users in the system.
    <% end %>
    <br />
    <%= link_to "sign Out", destroy_user_session_path, 'data-turbo-method': :delete %>
<% else %>
  <p> You are not signed.</p><br />
  <h3><%= link_to "Sing Up!", new_user_registration_path %></h3>
  <br />
  <%= link_to "Login", new_user_session_path %>
<% end %>
</div>